{
  "name": "vue-node",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start --workspace=backend\" \"npm run dev --workspace=frontend\""
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
